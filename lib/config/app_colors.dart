// app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromARGB(255, 200, 7, 23); // Teal
  static const primaryDark = Colors.black;
  static const primaryLight = Colors.white;
  // static const accent = Color(0xFFFF5252); // Red
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const background = Colors.black;
  static const card = Color(0xFF333333); // Dark grey for cards

  static const cardBackground = Color.fromARGB(137, 65, 10, 10);
  static const ratingColor = Color(0xFFFFC107); // Amber
}
