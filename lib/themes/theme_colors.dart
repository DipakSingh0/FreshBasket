import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const primary = Color.fromARGB(255, 200, 7, 23);
  static const lightPrimary = Color(0xFF6200EE);
  static const lightPrimaryVariant = Color(0xFF3700B3);
  static const lightSecondary = Color(0xFF03DAC6);
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightTextColor = Color(0xFF1E1E1E);
  static const lightContainerColor = Color(0xFFF5F5F5);
  static const lightContainerBackground = Color(0xFFF0F0F0);
  static const lightOnPrimary = Color(0xFFFFFFFF); //  buttons/text on primary
  static const lightOnSecondary = Color(
    0xFF000000,
  ); //  buttons/text on secondary
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const background = Colors.black;
  static const card = Color(0xFF333333);
  static const cardBackground = Color.fromARGB(137, 65, 10, 10);
  // Dark Theme Colors
  static const darkPrimary = Color(0xFFBB86FC);
  static const darkPrimaryVariant = Color(0xFF3700B3);
  static const darkSecondary = Color(0xFF03DAC6);
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkTextColor = Color(0xFFFFFFFF);
  static const darkContainerColor = Color(0xFF1E1E1E);
  static const darkContainerBackground = Color(0xFF121212);
  static const darkOnPrimary = Color(0xFF000000); // buttons/text on primary
  static const darkOnSecondary = Color(0xFF000000); // buttons/text on secondary
}
